<!DOCTYPE html>
<html class="h-full">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>
      {{ title }}
    </title>
    @vite(['resources/css/app.css', 'resources/js/app.js'])
  </head>
  <body class="w-screen min-h-full bg-gray-50">

    <header class="bg-white border-gray-200 dark:bg-gray-900">
      <nav class="max-w-4xl flex flex-wrap items-center justify-between mx-auto p-4">
        <a href="/" class="flex items-center space-x-3 rtl:space-x-reverse">
          <img src="/logo.png" class="h-8" alt="Rikjoy Academy Logo" />
        </a>

      </nav>
    </header>

    <main class="grow max-w-4xl mx-auto p-4">
      {{{ await $slots.main() }}}
    </main>

  </body>
</html>
